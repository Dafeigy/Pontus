# Pontus

Pontus 是一个使用Tauri2 + Vue3 构建的Litellm桌面端运维应用。它使用ShadCN-Vue组件进行前端构建，后端采用Rust构建。设计崇尚简洁、高级。

Pontus在首次启动需要进入两步引导设置：Litellm 连接配置 → SMTP 邮件配置（可跳过）。之后通过侧边栏导航进行用户邀请、看板查看和设置修改。

具体的功能，你可以参考../litellm-manager 这个目录。事实上，这个仓库已经实现了我想要的所有功能，我只是希望你可以在我给定的既定模板中将这些功能进行迁移。